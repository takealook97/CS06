import java.util.HashMap;

public class Cases {//기물이 갈 수 있는 경로 -> move제한할때도 쓰이고 ?xx입력일때도 쓰임
//    HashMap<기물정보(포지션,어떤기물인지,색깔), 갈 수 있는 포지션(배열)>
    void showMovablePosition(String input) {
        //HashMap에서 키값으로 value get 한 뒤 String으로 배열 출력
    }
    void putPieceToMap(String input){

    }
}
